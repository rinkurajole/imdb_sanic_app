version: '3.5'

services:
  api:	
    build: .
    ports:
      - 8080:8080
    links:
      - database
    restart: on-failure

  database:
    image: postgres
    env_file:
	- ./.env
    volumes:
        - database-data:/var/lib/postgresql/data/	